The data collection file is used for the connection to firebase but 
there is no particular command to run the scripts specifically because 
it works hand in hand with the other functionalities and not on its own. 

(The API KEY is included in the email sent)